## Before → after

<!-- What was wrong or missing, and what this PR changes — for a reader who
     hasn't seen the code. -->

## Proof

<!-- What was MEASURED, not argued: the test that fails without the change (the
     red seen), the probe, the screenshot. The suite's verdict is the
     "Test run with N tests" line, never xcodebuild's exit. -->

## Docs in the same PR

<!-- The comments of every touched passage, reconfirmed or rewritten; a new
     design decision, anchored in docs/DECISIONS.md; a touched UI string,
     through scripts/check-catalog.py. "None, because …" is also an answer. -->

## Honest residual

<!-- What stays open, and what was NOT proven. -->

Related issues: #
<!-- "Closes #N" only fires from the default branch (main), so a PR against dev
     never auto-closes an issue — cite it here, and the release that carries
     the fix to main closes it. -->
